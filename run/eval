#! /usr/bin/env node

require('console-ultimate/global').replace();

var load = JSON.parse;

var inputOrFile = require('../src/cli-util/input-or-file');

var Eval = require('../src/runtime/Eval');

inputOrFile(process.argv[2], function (ast)
{
	ast = load(ast);

	Eval(ast, process);
});
